[
 {
  "name": "account_check",
  "group": "accounts",
  "mutates": false,
  "scope": "mcp:read",
  "doc": "Жива перевірка акаунта (connect+get_me через його проксі, нічого не надсилає).\n\nref — id/номер/назва, або `all` / `active` / `problem` для групи.\n\nПозначений як такий, що лише читає: єдиний запис у БД — позначка «востаннє\nвикористано» для живих акаунтів, вона ні на що в роботі стадій не впливає.\n(На відміну від `proxy_check`, чий вердикт лягає в `is_working`/`fail_count`,\nзвідки воркери й беруть проксі.)",
  "summary": "Жива перевірка акаунта (connect+get_me через його проксі, нічого не надсилає).",
  "params": [
   {
    "name": "ref",
    "type": "str",
    "doc": "Акаунт: числовий id, номер телефону або частина назви. Для групових дій — ключові слова: all (усі), active (активні), problem (не авторизовані, обмежені SpamBot, без проксі або з мертвою).",
    "default": null,
    "required": true
   },
   {
    "name": "pause",
    "type": "float",
    "doc": "Пауза між акаунтами в секундах, щоб не бити всі одночасно. Перевірка одного акаунта — 5-20 с, тож групу більшу за 25 акаунтів інструмент не візьме (для `all` на великому пулі бери `problem`).",
    "default": 2.0,
    "required": false
   }
  ]
 },
 {
  "name": "account_dialogs",
  "group": "accounts",
  "mutates": false,
  "scope": "mcp:read",
  "doc": "Діалоги акаунта наживо (на що підписаний) — перевірка «прогрітості».\n\nkind — фільтр `канал` / `група` / `приват`.",
  "summary": "Діалоги акаунта наживо (на що підписаний) — перевірка «прогрітості».",
  "params": [
   {
    "name": "ref",
    "type": "str",
    "doc": "Акаунт: id, номер або частина назви (лише один, не група).",
    "default": null,
    "required": true
   },
   {
    "name": "limit",
    "type": "int",
    "doc": "Скільки діалогів показати.",
    "default": 40,
    "required": false
   },
   {
    "name": "kind",
    "type": "str",
    "doc": "Фільтр типу: канал | група | приват. Порожньо = усі.",
    "default": "",
    "required": false
   }
  ]
 },
 {
  "name": "account_import",
  "group": "accounts",
  "mutates": true,
  "scope": "mcp:create",
  "doc": "Додати Telegram-акаунт із tdata-експорту: <phone>.json + <phone>.session.\n\nТе саме, що адмінка «Додати акаунт через файли»: SQLiteSession → StringSession,\ndevice-відбиток переноситься, 2FA — з JSON. Сесія одразу вважається\nавторизованою; далі — `account_check` (жива?) і `account_spam_check`.\nСекрети (session, 2FA) у відповідь не потрапляють.",
  "summary": "Додати Telegram-акаунт із tdata-експорту: <phone>.json + <phone>.session.",
  "params": [
   {
    "name": "meta_json",
    "type": "str",
    "doc": "ВМІСТ файлу <phone>.json з tdata-експорту (JSON-текст як є: phone, app_id, app_hash, device, sdk, app_version, lang_pack, twoFA…).",
    "default": null,
    "required": true
   },
   {
    "name": "session_b64",
    "type": "str",
    "doc": "ВМІСТ файлу <phone>.session (Telethon SQLiteSession) у base64. Альтернатива — session_path.",
    "default": "",
    "required": false
   },
   {
    "name": "session_path",
    "type": "str",
    "doc": "Шлях до .session-файлу ВСЕРЕДИНІ контейнера web (напр. /app/backend/media/import/79990000000.session), якщо файл уже лежить на сервері. Альтернатива — session_b64.",
    "default": "",
    "required": false
   },
   {
    "name": "tags",
    "type": "str",
    "doc": "Теги акаунта через кому (створяться, якщо немає).",
    "default": "",
    "required": false
   },
   {
    "name": "proxy",
    "type": "str",
    "doc": "Проксі: id або частина рядка. Порожньо = призначить воркер/gateway. Без проксі акаунт не працює.",
    "default": "",
    "required": false
   },
   {
    "name": "shared",
    "type": "bool",
    "doc": "true — спільний акаунт (бачать усі); false = власник — ти. Суперюзер за замовчуванням додає спільний, решта — свій.",
    "default": null,
    "required": false
   }
  ]
 },
 {
  "name": "account_jobs",
  "group": "accounts",
  "mutates": false,
  "scope": "mcp:read",
  "doc": "Черги завдань акаунтів: `warm_up` (прогрів) і `test_bot` (тестовий прогін бота).",
  "summary": "Черги завдань акаунтів: `warm_up` (прогрів) і `test_bot` (тестовий прогін бота).",
  "params": [
   {
    "name": "kind",
    "type": "str",
    "doc": "Черга: all | warm_up (прогрів) | test_bot (тестовий прогін бота).",
    "default": "all",
    "required": false
   },
   {
    "name": "status",
    "type": "str",
    "doc": "Статус завдання. Прогрів: pending | running | done | failed. Тестовий прогін ще має queued і cancelled. Порожньо = усі.",
    "default": "",
    "required": false
   },
   {
    "name": "limit",
    "type": "int",
    "doc": "Скільки завдань показати.",
    "default": 20,
    "required": false
   }
  ]
 },
 {
  "name": "account_repair",
  "group": "accounts",
  "mutates": true,
  "scope": "mcp:write",
  "doc": "Полагодити акаунт через gateway: живість → регенерація session-id проксі →\nстан. Те саме робить фоновий ремонт gateway після транспортних збоїв.",
  "summary": "Полагодити акаунт через gateway: живість → регенерація session-id проксі → стан. Те саме робить фоновий ремонт gateway після транспортних збоїв.",
  "params": [
   {
    "name": "ref",
    "type": "str",
    "doc": "id/номер/назва, або `problem` — усі в cooldown / needs_proxy.",
    "default": null,
    "required": true
   }
  ]
 },
 {
  "name": "account_show",
  "group": "accounts",
  "mutates": false,
  "scope": "mcp:read",
  "doc": "Картка акаунта: конфіг, проксі, що він обслуговує, останні завдання й боти.",
  "summary": "Картка акаунта: конфіг, проксі, що він обслуговує, останні завдання й боти.",
  "params": [
   {
    "name": "ref",
    "type": "str",
    "doc": "Акаунт: числовий id, номер телефону або частина назви — ЛИШЕ ОДИН. Групові ключові слова (all/active/problem) тут не працюють: «all» шукатиметься як частина назви. Неоднозначність — відповість списком кандидатів.",
    "default": null,
    "required": true
   }
  ]
 },
 {
  "name": "account_spam_check",
  "group": "accounts",
  "mutates": true,
  "scope": "mcp:write",
  "doc": "Запитати @SpamBot про обмеження акаунта (пише боту /start) і зберегти статус.\n\nСаме обмеження SpamBot-ом, а не мертва проксі, валить резолв юзернеймів.\nref — id/номер/назва, або `all` / `active` / `problem`.",
  "summary": "Запитати @SpamBot про обмеження акаунта (пише боту /start) і зберегти статус.",
  "params": [
   {
    "name": "ref",
    "type": "str",
    "doc": "Акаунт: числовий id, номер телефону або частина назви. Для групових дій — ключові слова: all (усі), active (активні), problem (не авторизовані, обмежені SpamBot, без проксі або з мертвою).",
    "default": null,
    "required": true
   },
   {
    "name": "pause",
    "type": "float",
    "doc": "Пауза між акаунтами в секундах. Це НАДСИЛАННЯ повідомлення боту @SpamBot від імені акаунта, тож група обмежена 15.",
    "default": 2.0,
    "required": false
   }
  ]
 },
 {
  "name": "account_update",
  "group": "accounts",
  "mutates": true,
  "scope": "mcp:write",
  "doc": "Змінити акаунт: активність, проксі, теги (теги — через кому).\n\nproxy: id/частина рядка проксі, або `-` щоб відв'язати.",
  "summary": "Змінити акаунт: активність, проксі, теги (теги — через кому).",
  "params": [
   {
    "name": "ref",
    "type": "str",
    "doc": "Акаунт: числовий id, номер телефону або частина назви — ЛИШЕ ОДИН. Групові ключові слова (all/active/problem) тут не працюють: «all» шукатиметься як частина назви. Неоднозначність — відповість списком кандидатів.",
    "default": null,
    "required": true
   },
   {
    "name": "is_active",
    "type": "bool",
    "doc": "Увімкнути/вимкнути акаунт для роботи стадій.",
    "default": null,
    "required": false
   },
   {
    "name": "proxy",
    "type": "str",
    "doc": "Проксі: числовий id або частина рядка; '-' щоб відвʼязати.",
    "default": "",
    "required": false
   },
   {
    "name": "add_tags",
    "type": "str",
    "doc": "Теги через кому — додати (створюються за потреби).",
    "default": "",
    "required": false
   },
   {
    "name": "remove_tags",
    "type": "str",
    "doc": "Теги через кому — зняти.",
    "default": "",
    "required": false
   }
  ]
 },
 {
  "name": "account_warm_up",
  "group": "accounts",
  "mutates": true,
  "scope": "mcp:write",
  "doc": "Поставити акаунт(и) у чергу прогріву — підписка на випадкові канали з довідника.\n\nНепрогрітий акаунт не резолвить чужі юзернейми. Виконує воркер `warm-up`\n(join кожного каналу — секунди, тому це черга, а не синхронна дія).\nchannels — скільки каналів (0 = випадково 5-10).",
  "summary": "Поставити акаунт(и) у чергу прогріву — підписка на випадкові канали з довідника.",
  "params": [
   {
    "name": "ref",
    "type": "str",
    "doc": "Акаунт: числовий id, номер телефону або частина назви. Для групових дій — ключові слова: all (усі), active (активні), problem (не авторизовані, обмежені SpamBot, без проксі або з мертвою).",
    "default": null,
    "required": true
   },
   {
    "name": "channels",
    "type": "int",
    "doc": "На скільки каналів підписати. 0 = випадково 5-10.",
    "default": 0,
    "required": false
   }
  ]
 },
 {
  "name": "accounts_list",
  "group": "accounts",
  "mutates": false,
  "scope": "mcp:read",
  "doc": "Усі Telegram-акаунти: авторизація, статус SpamBot, проксі, навантаження.\n\nquery — частина назви/номера/тега; problems_only — лише ті, що потребують уваги\n(не авторизовані, обмежені SpamBot-ом, без проксі або з мертвою проксі).\n«чати»/«джерела» — скільки моніторингів читає саме цей акаунт.",
  "summary": "Усі Telegram-акаунти: авторизація, статус SpamBot, проксі, навантаження.",
  "params": [
   {
    "name": "query",
    "type": "str",
    "doc": "Частина назви, номера або тега акаунта. Порожньо = усі видимі.",
    "default": "",
    "required": false
   },
   {
    "name": "problems_only",
    "type": "bool",
    "doc": "true — лише ті, що потребують уваги: не авторизовані, обмежені SpamBot, без проксі або з мертвою проксі.",
    "default": false,
    "required": false
   },
   {
    "name": "limit",
    "type": "int",
    "doc": "Скільки акаунтів показати.",
    "default": 100,
    "required": false
   }
  ]
 },
 {
  "name": "proxies_list",
  "group": "accounts",
  "mutates": false,
  "scope": "mcp:read",
  "doc": "Пул проксі: хто живий, скільки збоїв, кому призначені.",
  "summary": "Пул проксі: хто живий, скільки збоїв, кому призначені.",
  "params": [
   {
    "name": "problems_only",
    "type": "bool",
    "doc": "true — лише мертві або зі збоями.",
    "default": false,
    "required": false
   },
   {
    "name": "limit",
    "type": "int",
    "doc": "Скільки проксі показати.",
    "default": 60,
    "required": false
   }
  ]
 },
 {
  "name": "proxy_check",
  "group": "accounts",
  "mutates": true,
  "scope": "mcp:write",
  "doc": "Перевірити проксі наживо; при збої — спробувати нову sticky-сесію (як воркер).\n\nref — id/частина рядка, або `all` / `broken`. repair=False — лише діагноз.",
  "summary": "Перевірити проксі наживо; при збої — спробувати нову sticky-сесію (як воркер).",
  "params": [
   {
    "name": "ref",
    "type": "str",
    "doc": "Проксі: числовий id або частина рядка; ключові слова all (усі активні) чи broken (лише мертві). Більше за 30 проксі за раз інструмент не візьме — до 12 с на перевірку.",
    "default": null,
    "required": true
   },
   {
    "name": "repair",
    "type": "bool",
    "doc": "true — при збої спробувати нову sticky-сесію і зберегти її. false — лише діагноз, але результат перевірки (жива/мертва, лічильник збоїв) усе одно записується, тому інструмент позначений як такий, що змінює стан.",
    "default": true,
    "required": false
   }
  ]
 },
 {
  "name": "channel_add",
  "group": "monitoring",
  "mutates": true,
  "scope": "mcp:create",
  "doc": "Додати канал/чат/сайт у довідник (`Channel`) або дозаповнити наявний.\n\nДовідник спільний: рядок ідентифікує посилання, тож повторний виклик не\nдублює, а віддає той самий рядок і дописує порожні поля й теми. Щоб канал\nще й ОПИТУВАВСЯ, потрібне джерело: `source_add` (воно саме створить рядок\nдовідника, якщо його нема). У whitelist моніторингу — через адмінку.",
  "summary": "Додати канал/чат/сайт у довідник (`Channel`) або дозаповнити наявний.",
  "params": [
   {
    "name": "url",
    "type": "str",
    "doc": "Посилання або @username: https://t.me/name, @name, https://vk.com/club…, https://site/розділ. Ключ довідника — нормалізоване посилання, дубль не створиться.",
    "default": null,
    "required": true
   },
   {
    "name": "title",
    "type": "str",
    "doc": "Назва (заповнює лише порожню).",
    "default": "",
    "required": false
   },
   {
    "name": "region",
    "type": "str",
    "doc": "Субʼєкт РФ канонічною назвою або аліасом із довідника регіонів (заповнює лише порожній).",
    "default": "",
    "required": false
   },
   {
    "name": "topics",
    "type": "str",
    "doc": "Теми/теги через кому (новини, етнічне, політика, локал-чат…). Додаються до наявних.",
    "default": "",
    "required": false
   },
   {
    "name": "chat_type",
    "type": "str",
    "doc": "channel | chat | discussion | unknown. Порожньо = за платформою.",
    "default": "",
    "required": false
   },
   {
    "name": "language",
    "type": "str",
    "doc": "Код мови (ru, uk…).",
    "default": "",
    "required": false
   }
  ]
 },
 {
  "name": "channel_update",
  "group": "monitoring",
  "mutates": true,
  "scope": "mcp:write",
  "doc": "Змінити рядок довідника: теми (теги), назву, регіон, населений пункт, тип, фокус.\n\nТеми — це поле `Channel.topics` (список), те саме, що править агент\nкласифікації директорії; підписники/активність/доступ пише лише код.",
  "summary": "Змінити рядок довідника: теми (теги), назву, регіон, населений пункт, тип, фокус.",
  "params": [
   {
    "name": "ref",
    "type": "str",
    "doc": "Канал довідника: id рядка (#123 із channels_find), @username, посилання або частина назви (має бути однозначною).",
    "default": null,
    "required": true
   },
   {
    "name": "add_topics",
    "type": "str",
    "doc": "Теми/теги додати (через кому).",
    "default": "",
    "required": false
   },
   {
    "name": "remove_topics",
    "type": "str",
    "doc": "Теми/теги прибрати (через кому).",
    "default": "",
    "required": false
   },
   {
    "name": "title",
    "type": "str",
    "doc": "Нова назва. Порожньо = не змінювати.",
    "default": "",
    "required": false
   },
   {
    "name": "region",
    "type": "str",
    "doc": "Субʼєкт РФ (канонічна назва/аліас). Порожньо = не змінювати; '-' = очистити.",
    "default": "",
    "required": false
   },
   {
    "name": "settlement",
    "type": "str",
    "doc": "Населений пункт. Порожньо = не змінювати; '-' = очистити.",
    "default": "",
    "required": false
   },
   {
    "name": "chat_type",
    "type": "str",
    "doc": "channel | chat | discussion | unknown. Порожньо = не змінювати.",
    "default": "",
    "required": false
   },
   {
    "name": "focus",
    "type": "str",
    "doc": "Фокус каналу одним реченням. Порожньо = не змінювати; '-' = очистити.",
    "default": "",
    "required": false
   },
   {
    "name": "discusses_problems",
    "type": "bool",
    "doc": "Чи обговорює суспільні проблеми РФ. Не передавати = не змінювати.",
    "default": null,
    "required": false
   }
  ]
 },
 {
  "name": "channels_find",
  "group": "monitoring",
  "mutates": false,
  "scope": "mcp:read",
  "doc": "Знайти канал/чат у нашому довіднику (`Channel`) за username/назвою.\n\nКолонка «id» — це id рядка довідника, і він НЕ підходить ні для\n`chat_update` (там id рядка whitelist із `chats_list`), ні для\n`tz_find(channel=…)` (там TelegramID або @username). Із цієї таблиці для\nінших інструментів бери @username, а рядок whitelist шукай у `chats_list`.",
  "summary": "Знайти канал/чат у нашому довіднику (`Channel`) за username/назвою.",
  "params": [
   {
    "name": "query",
    "type": "str",
    "doc": "@username або частина назви каналу в НАШОМУ довіднику. Пошук у базі TeleZip — це tz_channels.",
    "default": null,
    "required": true
   },
   {
    "name": "limit",
    "type": "int",
    "doc": "Скільки каналів показати.",
    "default": 20,
    "required": false
   }
  ]
 },
 {
  "name": "chat_update",
  "group": "monitoring",
  "mutates": true,
  "scope": "mcp:write",
  "doc": "Змінити рядок whitelist: активність, режим стріму, акаунт збору, пріоритет.\n\nchat — id рядка або @username (якщо однозначний). account — id/назва акаунта\nабо `-` щоб відв'язати.",
  "summary": "Змінити рядок whitelist: активність, режим стріму, акаунт збору, пріоритет.",
  "params": [
   {
    "name": "chat",
    "type": "str",
    "doc": "Рядок whitelist: числовий id із chats_list або @username чату.",
    "default": null,
    "required": true
   },
   {
    "name": "is_active",
    "type": "bool",
    "doc": "Увімкнути/вимкнути чат для наступних зборів (історія лишається).",
    "default": null,
    "required": false
   },
   {
    "name": "stream_enabled",
    "type": "bool",
    "doc": "true — читати чат суцільно (стрім), false — шукати за словами.",
    "default": null,
    "required": false
   },
   {
    "name": "account",
    "type": "str",
    "doc": "Telegram-акаунт збору: id/номер/назва, або '-' щоб відвʼязати.",
    "default": "",
    "required": false
   },
   {
    "name": "priority",
    "type": "int",
    "doc": "Менше число = вище в списку.",
    "default": null,
    "required": false
   },
   {
    "name": "forward_media",
    "type": "bool",
    "doc": "Пересилати медіа цього чату в чат медіа задачі.",
    "default": null,
    "required": false
   }
  ]
 },
 {
  "name": "chats_list",
  "group": "monitoring",
  "mutates": false,
  "scope": "mcp:read",
  "doc": "Whitelist чатів моніторингу: акаунт збору, режим (стрім/пошук), свіжість.\n\nproblems_only — без акаунта, або стрім не оновлювався понад добу.",
  "summary": "Whitelist чатів моніторингу: акаунт збору, режим (стрім/пошук), свіжість.",
  "params": [
   {
    "name": "task",
    "type": "str",
    "doc": "Задача: числовий id, slug або частина назви. Неоднозначність або чужа задача — відповість «не знайдено».",
    "default": "",
    "required": false
   },
   {
    "name": "active",
    "type": "bool",
    "doc": "true — лише активні рядки whitelist, false — лише вимкнені, не передавати — усі.",
    "default": null,
    "required": false
   },
   {
    "name": "stream_only",
    "type": "bool",
    "doc": "true — лише чати в режимі стріму (полінг історії).",
    "default": false,
    "required": false
   },
   {
    "name": "problems_only",
    "type": "bool",
    "doc": "true — лише проблемні: без акаунта збору або стрім не оновлювався понад добу.",
    "default": false,
    "required": false
   },
   {
    "name": "limit",
    "type": "int",
    "doc": "Скільки рядків показати.",
    "default": 60,
    "required": false
   }
  ]
 },
 {
  "name": "event_add",
  "group": "monitoring",
  "mutates": true,
  "scope": "mcp:write",
  "doc": "Додати подію за посиланням — як «Додати подію» в списку подій адмінки.\n\nТекст поста/статті → скрін-промпт дослідження (relevant/summary/region/tags)\n→ Post(done) → Event(approved). Це виклик LLM і fetch сторінки; закритий\nканал або нерелевантний текст → зрозуміла помилка. Якщо пост із таким URL\nу дослідженні вже має подію — повернеться вона, дубля не буде.",
  "summary": "Додати подію за посиланням — як «Додати подію» в списку подій адмінки.",
  "params": [
   {
    "name": "task",
    "type": "str",
    "doc": "Задача (дослідження): id, slug або частина назви — лише своя.",
    "default": null,
    "required": true
   },
   {
    "name": "url",
    "type": "str",
    "doc": "Посилання на пост Telegram (https://t.me/name/123, відкритий канал) або статтю на сайті.",
    "default": null,
    "required": true
   }
  ]
 },
 {
  "name": "event_show",
  "group": "monitoring",
  "mutates": false,
  "scope": "mcp:read",
  "doc": "Картка події: опис, регіон, усі теги, аудит, пости-джерела з посиланнями.",
  "summary": "Картка події: опис, регіон, усі теги, аудит, пости-джерела з посиланнями.",
  "params": [
   {
    "name": "ref",
    "type": "str",
    "doc": "id події (з events_list).",
    "default": null,
    "required": true
   }
  ]
 },
 {
  "name": "event_update",
  "group": "monitoring",
  "mutates": true,
  "scope": "mcp:write",
  "doc": "Схвалити/відхилити/повернути в чергу, тегувати, поправити регіон/дату/опис події.\n\nСхвалення/відхилення — те саме, що дії «✅ Схвалити» / «🚫 Відхилити» в\nадмінці (нотатка `manual: … by <user>`, claim-lock аудиту знімається).\nВідхилена подія зникає з дефолтних списків і графіків і згодом видаляється\nчисткою — це не «м'яке приховування».",
  "summary": "Схвалити/відхилити/повернути в чергу, тегувати, поправити регіон/дату/опис події.",
  "params": [
   {
    "name": "ref",
    "type": "str",
    "doc": "id події (з events_list).",
    "default": null,
    "required": true
   },
   {
    "name": "review",
    "type": "str",
    "doc": "approve — схвалити; reject — відхилити (буде видалено чисткою); pending — повернути в чергу аудиту. Порожньо = не чіпати.",
    "default": "",
    "required": false
   },
   {
    "name": "notes",
    "type": "str",
    "doc": "Нотатка аудиту (чому). Пишеться разом із review або окремо.",
    "default": "",
    "required": false
   },
   {
    "name": "add_tags",
    "type": "str",
    "doc": "Теги додати: `категорія:тег` через кому (напр. `topic:мігранти, attacker_nationality:узбек`). Категорія обовʼязкова; у закритій категорії тег має бути зі словника, у відкритій — канонізується (можливий виклик LLM) або створюється.",
    "default": "",
    "required": false
   },
   {
    "name": "remove_tags",
    "type": "str",
    "doc": "Теги прибрати: `категорія:тег` або `тег` через кому.",
    "default": "",
    "required": false
   },
   {
    "name": "region",
    "type": "str",
    "doc": "Субʼєкт РФ (канонічна назва/аліас). Порожньо = не змінювати; '-' = очистити.",
    "default": "",
    "required": false
   },
   {
    "name": "settlement",
    "type": "str",
    "doc": "Населений пункт. Порожньо = не змінювати; '-' = очистити.",
    "default": "",
    "required": false
   },
   {
    "name": "event_date",
    "type": "str",
    "doc": "Дата події YYYY-MM-DD. Порожньо = не змінювати.",
    "default": "",
    "required": false
   },
   {
    "name": "summary",
    "type": "str",
    "doc": "Новий опис події. Порожньо = не змінювати.",
    "default": "",
    "required": false
   }
  ]
 },
 {
  "name": "events_list",
  "group": "monitoring",
  "mutates": false,
  "scope": "mcp:read",
  "doc": "Список подій із фільтрами адмінки: період/свіжість, задача, статус аудиту,\nрегіон, теги (фасети), канал, кількість каналів, охоплення.\n\nДефолт — як у списку адмінки: лише «Схвалено» за останні 30 днів;\n`review_status=pending` — черга на аудит (далі `event_update` схвалює/відхиляє).\nКатегорії тегів для фільтра `tag` — `tag_categories`; id події — для\n`event_show`/`event_update`.",
  "summary": "Список подій із фільтрами адмінки: період/свіжість, задача, статус аудиту, регіон, теги (фасети), канал, кількість каналів, охоплення.",
  "params": [
   {
    "name": "task",
    "type": "str",
    "doc": "Задача (id/slug/назва — лише своя). Порожньо = усі свої.",
    "default": "",
    "required": false
   },
   {
    "name": "days",
    "type": "int",
    "doc": "Останні N днів за датою події (як «Свіжість» в адмінці). Ігнорується, якщо задано date_from/date_to.",
    "default": 30,
    "required": false
   },
   {
    "name": "date_from",
    "type": "str",
    "doc": "Дата події від, YYYY-MM-DD (як фільтр «Період»).",
    "default": "",
    "required": false
   },
   {
    "name": "date_to",
    "type": "str",
    "doc": "Дата події до, YYYY-MM-DD.",
    "default": "",
    "required": false
   },
   {
    "name": "review_status",
    "type": "str",
    "doc": "approved (дефолт, як в адмінці) | pending | rejected | all.",
    "default": "approved",
    "required": false
   },
   {
    "name": "region",
    "type": "str",
    "doc": "Субʼєкт РФ (канонічна назва/аліас) — фільтр «Субʼєкт РФ».",
    "default": "",
    "required": false
   },
   {
    "name": "settlement",
    "type": "str",
    "doc": "Населений пункт (частина назви).",
    "default": "",
    "required": false
   },
   {
    "name": "tag",
    "type": "str",
    "doc": "Теги: `категорія:тег` або просто `тег`; кілька через кому = ВСІ мають бути (як фасети в адмінці). Категорії: tag_categories або task_show.",
    "default": "",
    "required": false
   },
   {
    "name": "query",
    "type": "str",
    "doc": "Текст в описі події (icontains).",
    "default": "",
    "required": false
   },
   {
    "name": "channel",
    "type": "str",
    "doc": "@username каналу/джерела, який писав про подію (фільтр «Канал/Джерело»).",
    "default": "",
    "required": false
   },
   {
    "name": "min_channels",
    "type": "int",
    "doc": "Мінімум унікальних каналів події (фільтр «Кількість каналів»). 0 = без обмеження.",
    "default": 0,
    "required": false
   },
   {
    "name": "min_reach",
    "type": "int",
    "doc": "Мінімальне охоплення. 0 = без обмеження.",
    "default": 0,
    "required": false
   },
   {
    "name": "order",
    "type": "str",
    "doc": "newest (дефолт) | oldest | reach | channels.",
    "default": "newest",
    "required": false
   },
   {
    "name": "limit",
    "type": "int",
    "doc": "Скільки подій показати.",
    "default": 30,
    "required": false
   }
  ]
 },
 {
  "name": "events_stats",
  "group": "monitoring",
  "mutates": false,
  "scope": "mcp:read",
  "doc": "Зріз подій: по днях/тижнях/місяцях, регіонах, тегах або задачах.\n\ngroup_by: day|week|month|region|tag:<категорія>|task.\nРахує через services/metrics.py — ті самі формули, що й графіки адмінки.",
  "summary": "Зріз подій: по днях/тижнях/місяцях, регіонах, тегах або задачах.",
  "params": [
   {
    "name": "task",
    "type": "str",
    "doc": "Задача: числовий id, slug або частина назви. Неоднозначність або чужа задача — відповість «не знайдено».",
    "default": "",
    "required": false
   },
   {
    "name": "days",
    "type": "int",
    "doc": "Скільки останніх діб узяти.",
    "default": 14,
    "required": false
   },
   {
    "name": "group_by",
    "type": "str",
    "doc": "Розріз: day | week | month | region | task | tag:<ключ категорії> (напр. tag:importance). Ключі категорій окремим інструментом не віддаються — їх видно в адмінці, /admin/analysis/tagcategory/.",
    "default": "day",
    "required": false
   },
   {
    "name": "region",
    "type": "str",
    "doc": "Назва субʼєкта РФ або її частина (напр. Дагестан).",
    "default": "",
    "required": false
   },
   {
    "name": "limit",
    "type": "int",
    "doc": "Скільки рядків показати.",
    "default": 20,
    "required": false
   },
   {
    "name": "review_status",
    "type": "str",
    "doc": "Статус аудиту подій: approved (дефолт) | pending | rejected | all.",
    "default": "approved",
    "required": false
   }
  ]
 },
 {
  "name": "run_cancel",
  "group": "monitoring",
  "mutates": true,
  "scope": "mcp:write",
  "doc": "Скасувати збір: статус `cancelled` + (опційно) прибрати його ще не взяті чанки.",
  "summary": "Скасувати збір: статус `cancelled` + (опційно) прибрати його ще не взяті чанки.",
  "params": [
   {
    "name": "run_id",
    "type": "int",
    "doc": "Числовий id збору.",
    "default": null,
    "required": true
   },
   {
    "name": "drop_pending_chunks",
    "type": "bool",
    "doc": "true — прибрати ще не взяті чанки з черги. Уже зібрані пости лишаються в конвеєрі в будь-якому разі.",
    "default": true,
    "required": false
   }
  ]
 },
 {
  "name": "run_create",
  "group": "monitoring",
  "mutates": true,
  "scope": "mcp:write",
  "doc": "Запустити збір за період — створює ResearchRun і планує чанки (як «Збори → Додати»).\n\nДалі все ведуть воркери. Ідемпотентно за діапазонами: вже покриті чанки не дублюються.",
  "summary": "Запустити збір за період — створює ResearchRun і планує чанки (як «Збори → Додати»).",
  "params": [
   {
    "name": "task",
    "type": "str",
    "doc": "Задача: числовий id, slug або частина назви. Неоднозначність або чужа задача — відповість «не знайдено».",
    "default": null,
    "required": true
   },
   {
    "name": "date_from",
    "type": "str",
    "doc": "Перший день періоду, формат YYYY-MM-DD (напр. 2026-09-01).",
    "default": null,
    "required": true
   },
   {
    "name": "date_to",
    "type": "str",
    "doc": "Останній день періоду, YYYY-MM-DD, ВКЛЮЧНО. Межа — 400 днів на один збір.",
    "default": null,
    "required": true
   },
   {
    "name": "chunk_days",
    "type": "int",
    "doc": "Скільки днів в одному чанку TeleZip. 0 = взяти з налаштувань задачі. ПРЯМИЙ МНОЖНИК ЦІНИ: 1 запит (~$0.10) на чанк, тож 30 днів по дню = $3.00, по 3 дні = $1.00 — але більший чанк ближчий до відлупу.",
    "default": 0,
    "required": false
   },
   {
    "name": "title",
    "type": "str",
    "doc": "Необовʼязкова назва збору для списку.",
    "default": "",
    "required": false
   }
  ]
 },
 {
  "name": "run_show",
  "group": "monitoring",
  "mutates": false,
  "scope": "mcp:read",
  "doc": "Збір детально: чанки, рух постів по стадіях, події періоду (як екран «Збори → Статус»).",
  "summary": "Збір детально: чанки, рух постів по стадіях, події періоду (як екран «Збори → Статус»).",
  "params": [
   {
    "name": "run_id",
    "type": "int",
    "doc": "Числовий id збору зі списку runs_list.",
    "default": null,
    "required": true
   }
  ]
 },
 {
  "name": "runs_list",
  "group": "monitoring",
  "mutates": false,
  "scope": "mcp:read",
  "doc": "Збори (ResearchRun): статус, період, прогрес чанків.",
  "summary": "Збори (ResearchRun): статус, період, прогрес чанків.",
  "params": [
   {
    "name": "task",
    "type": "str",
    "doc": "Задача: числовий id, slug або частина назви. Неоднозначність або чужа задача — відповість «не знайдено».",
    "default": "",
    "required": false
   },
   {
    "name": "status",
    "type": "str",
    "doc": "Статус збору: pending | collecting | collected | awaiting_agent | done | failed | cancelled. Порожньо = усі.",
    "default": "",
    "required": false
   },
   {
    "name": "limit",
    "type": "int",
    "doc": "Скільки зборів показати.",
    "default": 15,
    "required": false
   }
  ]
 },
 {
  "name": "source_add",
  "group": "monitoring",
  "mutates": true,
  "scope": "mcp:create",
  "doc": "Створити джерело інформпростору (`Source`) і, за потреби, підписати на нього задачу.\n\nДжерело = рядок довідника (`Channel`, створюється/знаходиться за посиланням)\n+ розклад/курсор/акаунт. Без підписки активної infospace-задачі\n`info_collect` його НЕ бере в роботу — тож зазвичай передавай `task`.\nПовторний виклик з тим самим посиланням віддає наявне джерело.",
  "summary": "Створити джерело інформпростору (`Source`) і, за потреби, підписати на нього задачу.",
  "params": [
   {
    "name": "url",
    "type": "str",
    "doc": "Посилання: https://t.me/name або @name (Telegram), https://site/section або RSS-адреса (сайт/RSS), https://vk.com/… (VK).",
    "default": null,
    "required": true
   },
   {
    "name": "kind",
    "type": "str",
    "doc": "telegram | rss | web | vk. Порожньо = за посиланням (t.me → telegram, vk.com → vk, інакше web; '.xml'/'/rss'/'/feed' → rss).",
    "default": "",
    "required": false
   },
   {
    "name": "task",
    "type": "str",
    "doc": "Одразу підписати задачу (id/slug/назва — лише свою). Порожньо = джерело без підписок (воркер його не опитуватиме).",
    "default": "",
    "required": false
   },
   {
    "name": "name",
    "type": "str",
    "doc": "Назва (заповнює лише порожню в довіднику).",
    "default": "",
    "required": false
   },
   {
    "name": "region",
    "type": "str",
    "doc": "Субʼєкт РФ канонічною назвою/аліасом.",
    "default": "",
    "required": false
   },
   {
    "name": "language",
    "type": "str",
    "doc": "Код мови (ru, uk…).",
    "default": "",
    "required": false
   },
   {
    "name": "poll_interval_sec",
    "type": "int",
    "doc": "Інтервал полінгу в секундах. 0 = дефолт моделі.",
    "default": 0,
    "required": false
   },
   {
    "name": "account",
    "type": "str",
    "doc": "Telegram-акаунт для читання (id/номер/назва) — лише для kind=telegram.",
    "default": "",
    "required": false
   }
  ]
 },
 {
  "name": "source_subscribe",
  "group": "monitoring",
  "mutates": true,
  "scope": "mcp:write",
  "doc": "Підписати задачу на джерело (або вимкнути підписку) — `SourceSubscription`.\n\nСаме підписка робить джерело «робочим» для `info_collect`. Вимкнення не\nвидаляє ні джерело, ні зібране — лише виключає з наступних зборів.",
  "summary": "Підписати задачу на джерело (або вимкнути підписку) — `SourceSubscription`.",
  "params": [
   {
    "name": "ref",
    "type": "str",
    "doc": "Джерело: id, посилання або частина назви.",
    "default": null,
    "required": true
   },
   {
    "name": "task",
    "type": "str",
    "doc": "Задача (id/slug/назва — лише своя).",
    "default": null,
    "required": true
   },
   {
    "name": "active",
    "type": "bool",
    "doc": "true — підписати/увімкнути; false — вимкнути підписку (історія лишається).",
    "default": true,
    "required": false
   },
   {
    "name": "priority",
    "type": "int",
    "doc": "Пріоритет підписки (менше = вище). 0 = не змінювати.",
    "default": 0,
    "required": false
   }
  ]
 },
 {
  "name": "source_update",
  "group": "monitoring",
  "mutates": true,
  "scope": "mcp:write",
  "doc": "Змінити джерело: активність, інтервал, «опитати зараз», скидання курсора (backfill).",
  "summary": "Змінити джерело: активність, інтервал, «опитати зараз», скидання курсора (backfill).",
  "params": [
   {
    "name": "ref",
    "type": "str",
    "doc": "Джерело: числовий id, частина URL або назви.",
    "default": null,
    "required": true
   },
   {
    "name": "is_active",
    "type": "bool",
    "doc": "Увімкнути/вимкнути опитування джерела.",
    "default": null,
    "required": false
   },
   {
    "name": "poll_interval_sec",
    "type": "int",
    "doc": "Інтервал полінгу в секундах (нижня межа 60).",
    "default": null,
    "required": false
   },
   {
    "name": "poll_now",
    "type": "bool",
    "doc": "true — поставити джерело в чергу негайно.",
    "default": false,
    "required": false
   },
   {
    "name": "reset_cursor",
    "type": "bool",
    "doc": "true — забути, докуди вже прочитано, і перечитати заново. Може дати вал постів.",
    "default": false,
    "required": false
   },
   {
    "name": "account",
    "type": "str",
    "doc": "Telegram-акаунт для полінгу (для kind=telegram): id/номер/назва або '-'.",
    "default": "",
    "required": false
   }
  ]
 },
 {
  "name": "sources_list",
  "group": "monitoring",
  "mutates": false,
  "scope": "mcp:read",
  "doc": "Джерела інформпростору: розклад полінгу, health, якість, до яких задач підключені.",
  "summary": "Джерела інформпростору: розклад полінгу, health, якість, до яких задач підключені.",
  "params": [
   {
    "name": "task",
    "type": "str",
    "doc": "Задача: числовий id, slug або частина назви. Неоднозначність або чужа задача — відповість «не знайдено».",
    "default": "",
    "required": false
   },
   {
    "name": "kind",
    "type": "str",
    "doc": "Тип джерела: telegram | rss | web | vk. Порожньо = усі.",
    "default": "",
    "required": false
   },
   {
    "name": "problems_only",
    "type": "bool",
    "doc": "true — лише проблемні: збої, підозра на якість або прострочений полінг (рахується тільки серед тих, кого воркер реально бере).",
    "default": false,
    "required": false
   },
   {
    "name": "limit",
    "type": "int",
    "doc": "Скільки джерел показати.",
    "default": 60,
    "required": false
   }
  ]
 },
 {
  "name": "tag_categories",
  "group": "monitoring",
  "mutates": false,
  "scope": "mcp:read",
  "doc": "Категорії тегів (`TagCategory`) і приклади тегів — щоб правильно писати\n`tag=` у events_list і `add_tags=` в event_update. Закрита категорія =\nлише словникові теги.",
  "summary": "Категорії тегів (`TagCategory`) і приклади тегів — щоб правильно писати `tag=` у events_list і `add_tags=` в event_update. Закрита категорія = лише словникові теги.",
  "params": [
   {
    "name": "task",
    "type": "str",
    "doc": "Задача — показати лише її категорії (порожньо = усі) і по 12 найчастіших тегів кожної.",
    "default": "",
    "required": false
   }
  ]
 },
 {
  "name": "task_create",
  "group": "monitoring",
  "mutates": true,
  "scope": "mcp:create",
  "doc": "Створити дослідження (`AnalysisTask`) — власник = ти, далі воно видиме\nлише тобі (і суперюзерам), як в адмінці.\n\nМінімум — slug, назва і конвеєр; решту (запит, промпти, стадії) правиш\n`task_update`, канали/джерела підключаєш `source_add`/`source_subscribe`,\nзбір запускаєш `run_create`. Промпти класифікації за замовчуванням — із коду.",
  "summary": "Створити дослідження (`AnalysisTask`) — власник = ти, далі воно видиме лише тобі (і суперюзерам), як в адмінці.",
  "params": [
   {
    "name": "slug",
    "type": "str",
    "doc": "Ідентифікатор латиницею (a-z, 0-9, дефіс), унікальний: `migrants-kavkaz-2026`.",
    "default": null,
    "required": true
   },
   {
    "name": "name",
    "type": "str",
    "doc": "Назва дослідження.",
    "default": null,
    "required": true
   },
   {
    "name": "pipeline",
    "type": "str",
    "doc": "events (події з TeleZip-збору) | infospace (полінг джерел → живі події) | monitor (критика в чатах) | research (тематичне) | tgsearch (пошук у чатах Telegram). Дефолт events.",
    "default": "events",
    "required": false
   },
   {
    "name": "description",
    "type": "str",
    "doc": "Опис (для людей).",
    "default": "",
    "required": false
   },
   {
    "name": "telezip_query",
    "type": "str",
    "doc": "Запит TeleZip для збору (діалект v3: пробіл = АБО, І — `+`). Для infospace не потрібен.",
    "default": "",
    "required": false
   },
   {
    "name": "languages",
    "type": "str",
    "doc": "Мови через кому (ru, uk…).",
    "default": "",
    "required": false
   },
   {
    "name": "tag_categories",
    "type": "str",
    "doc": "Ключі категорій тегів через кому (див. tag_categories) — які теги класифікатор збирає з поста.",
    "default": "",
    "required": false
   },
   {
    "name": "display_name",
    "type": "str",
    "doc": "Людська назва для простого інтерфейсу (/app/).",
    "default": "",
    "required": false
   },
   {
    "name": "is_active",
    "type": "bool",
    "doc": "Створити активною (дефолт true).",
    "default": true,
    "required": false
   }
  ]
 },
 {
  "name": "task_show",
  "group": "monitoring",
  "mutates": false,
  "scope": "mcp:read",
  "doc": "Картка моніторингу: конфіг конвеєра, підключення, черги, події, останні збори.",
  "summary": "Картка моніторингу: конфіг конвеєра, підключення, черги, події, останні збори.",
  "params": [
   {
    "name": "ref",
    "type": "str",
    "doc": "Задача: числовий id, slug або частина назви. Неоднозначність або чужа задача — відповість «не знайдено».",
    "default": null,
    "required": true
   }
  ]
 },
 {
  "name": "task_update",
  "group": "monitoring",
  "mutates": true,
  "scope": "mcp:write",
  "doc": "Змінити параметри задачі: збір (запит TeleZip, мови, unique, чанк), назву/опис,\nпрапорці стадій (пости/коментарі, гео, аудит), вікно дедупу, промпт класифікації.\n\nЗамикає маршрут розвідки: `tz_find(stats=true)` показав, що обсяг здоровий →\nфіксуємо його в задачі → `run_create` збирає ним period. Старий запит\nдрукується у відповіді — щоб було куди відкотитись.",
  "summary": "Змінити параметри задачі: збір (запит TeleZip, мови, unique, чанк), назву/опис, прапорці стадій (пости/коментарі, гео, аудит), вікно дедупу, промпт класифікації.",
  "params": [
   {
    "name": "ref",
    "type": "str",
    "doc": "Задача: числовий id, slug або частина назви. Неоднозначність або чужа задача — відповість «не знайдено».",
    "default": null,
    "required": true
   },
   {
    "name": "telezip_query",
    "type": "str",
    "doc": "Новий пошуковий запит задачі. Порожньо = не змінювати; щоб ОЧИСТИТИ запит, передай '-'. УВАГА, ДІАЛЕКТ ІНШИЙ, НІЖ У tz_find: збір ходить у v3, де ПРОБІЛ = АБО, а І — це `+` перед групою (`тема +(дія) -(шум)`). У tz_find (v4) навпаки. Запит, перенесений звідси в tz_find без переписування, тихо дасть 0 збігів, і навпаки.",
    "default": "",
    "required": false
   },
   {
    "name": "languages",
    "type": "str",
    "doc": "Мови через кому (ru, uk…). Порожньо = не змінювати; очистити список звідси не можна — це робиться в адмінці задачі.",
    "default": "",
    "required": false
   },
   {
    "name": "unique",
    "type": "bool",
    "doc": "Згортати репости при зборі. Не передавати = не змінювати.",
    "default": null,
    "required": false
   },
   {
    "name": "chunk_days",
    "type": "int",
    "doc": "Розмір чанка збору в днях. 0 = не змінювати.",
    "default": 0,
    "required": false
   },
   {
    "name": "is_active",
    "type": "bool",
    "doc": "Увімкнути/вимкнути задачу. Не передавати = не змінювати.",
    "default": null,
    "required": false
   },
   {
    "name": "min_subscribers",
    "type": "int",
    "doc": "Відсівати канали, менші за це число підписників. -1 = не змінювати, 0 = вимкнути фільтр.",
    "default": -1,
    "required": false
   },
   {
    "name": "llm_model",
    "type": "str",
    "doc": "Перевизначити модель LLM для задачі. Порожньо = не змінювати; повернути дефолт із коду звідси не можна — це робиться в адмінці задачі.",
    "default": "",
    "required": false
   },
   {
    "name": "display_name",
    "type": "str",
    "doc": "Людська назва секції для простого інтерфейсу (/app/), напр. «Інформпростір регіонів». Порожньо = не змінювати; '-' = очистити (показуватиметься технічна назва).",
    "default": "",
    "required": false
   },
   {
    "name": "name",
    "type": "str",
    "doc": "Технічна назва задачі. Порожньо = не змінювати.",
    "default": "",
    "required": false
   },
   {
    "name": "description",
    "type": "str",
    "doc": "Опис задачі (для людей). Порожньо = не змінювати; '-' = очистити.",
    "default": "",
    "required": false
   },
   {
    "name": "search_posts",
    "type": "bool",
    "doc": "Збирати пости каналів. Не передавати = не змінювати.",
    "default": null,
    "required": false
   },
   {
    "name": "search_comments",
    "type": "bool",
    "doc": "Збирати коментарі/повідомлення чатів. Не передавати = не змінювати.",
    "default": null,
    "required": false
   },
   {
    "name": "geo_enabled",
    "type": "bool",
    "doc": "Визначати регіон подій (гео-стадія). Не передавати = не змінювати.",
    "default": null,
    "required": false
   },
   {
    "name": "review_enabled",
    "type": "bool",
    "doc": "Авто-аудит подій LLM після створення. Не передавати = не змінювати.",
    "default": null,
    "required": false
   },
   {
    "name": "dedup_window_days",
    "type": "int",
    "doc": "Вікно дедупу подій у днях. 0 = не змінювати.",
    "default": 0,
    "required": false
   },
   {
    "name": "classify_prompt",
    "type": "str",
    "doc": "System-промпт класифікації постів (повний текст). Порожньо = не змінювати; '-' = повернути дефолт із коду. Перед зміною подивись поточний: task_show.",
    "default": "",
    "required": false
   }
  ]
 },
 {
  "name": "tasks_list",
  "group": "monitoring",
  "mutates": false,
  "scope": "mcp:read",
  "doc": "Задачі аналізу (моніторинги): конвеєр, обсяги, що до них підключено.",
  "summary": "Задачі аналізу (моніторинги): конвеєр, обсяги, що до них підключено.",
  "params": [
   {
    "name": "pipeline",
    "type": "str",
    "doc": "Конвеєр: events | monitor | research | infospace | tgsearch. Порожньо = усі.",
    "default": "",
    "required": false
   },
   {
    "name": "active_only",
    "type": "bool",
    "doc": "true — лише активні задачі.",
    "default": false,
    "required": false
   }
  ]
 },
 {
  "name": "publish_config_create",
  "group": "service",
  "mutates": true,
  "scope": "mcp:create",
  "doc": "Створити профіль публікації подій у Telegram-канал (власник = ти).\n\nЗа замовчуванням НЕАКТИВНИЙ — спершу перевір відбір `events_list` із тими\nсамими тегами/регіоном, потім `publish_config_update(is_active=true)`.\nАктивний профіль воркер `publish` бере одразу і публікує реальні пости.",
  "summary": "Створити профіль публікації подій у Telegram-канал (власник = ти).",
  "params": [
   {
    "name": "name",
    "type": "str",
    "doc": "Назва профілю.",
    "default": null,
    "required": true
   },
   {
    "name": "chat_id",
    "type": "str",
    "doc": "Chat ID каналу Telegram: -100… або @username.",
    "default": null,
    "required": true
   },
   {
    "name": "task",
    "type": "str",
    "doc": "Дослідження (id/slug/назва — своє). '-' = будь-яке (усі свої події).",
    "default": "",
    "required": false
   },
   {
    "name": "bot_token",
    "type": "str",
    "doc": "Bot token (порожньо = глобальний з env). У відповідях не показується.",
    "default": "",
    "required": false
   },
   {
    "name": "is_active",
    "type": "bool",
    "doc": "Активний профіль (воркер публікує лише активні).",
    "default": false,
    "required": false
   },
   {
    "name": "review_status",
    "type": "str",
    "doc": "Публікувати події зі статусом: approved (дефолт) | pending | rejected.",
    "default": "",
    "required": false
   },
   {
    "name": "tags",
    "type": "str",
    "doc": "Теги-відбір (АБО): `кат:тег, кат:тег`. '-' = без обмеження.",
    "default": "",
    "required": false
   },
   {
    "name": "require_tags",
    "type": "str",
    "doc": "Обовʼязкові теги (І): `кат:тег, …`. '-' = очистити.",
    "default": "",
    "required": false
   },
   {
    "name": "exclude_tags",
    "type": "str",
    "doc": "Теги-виключення: подія з будь-яким із них не публікується. '-' = очистити.",
    "default": "",
    "required": false
   },
   {
    "name": "regions",
    "type": "str",
    "doc": "Субʼєкти РФ через кому (канонічна назва/аліас). '-' = усі.",
    "default": "",
    "required": false
   },
   {
    "name": "max_age_days",
    "type": "int",
    "doc": "Не публікувати старше N діб. 0 = без обмеження; -1 = не змінювати.",
    "default": -1,
    "required": false
   },
   {
    "name": "publish_from",
    "type": "str",
    "doc": "Публікувати події від дати YYYY-MM-DD. '-' = зняти.",
    "default": "",
    "required": false
   },
   {
    "name": "raw_mode",
    "type": "bool",
    "doc": "true — без ШІ: оригінальний текст + теги; AI-промпт тоді лише фільтр.",
    "default": null,
    "required": false
   },
   {
    "name": "ai_model",
    "type": "str",
    "doc": "AI-модель (порожньо = дефолт). '-' = скинути.",
    "default": "",
    "required": false
   },
   {
    "name": "ai_prompt",
    "type": "str",
    "doc": "AI-промпт (фільтр + рерайт). '-' = очистити (у raw_mode = LLM не викликається).",
    "default": "",
    "required": false
   },
   {
    "name": "max_per_pass",
    "type": "int",
    "doc": "Макс. постів за прохід воркера. 0 = не змінювати.",
    "default": 0,
    "required": false
   },
   {
    "name": "post_as_account",
    "type": "bool",
    "doc": "true — постити Telegram-акаунтом (з медіа), потрібен forward_account.",
    "default": null,
    "required": false
   },
   {
    "name": "forward_account",
    "type": "str",
    "doc": "Акаунт публікації (id/номер/назва — видимий тобі). '-' = зняти.",
    "default": "",
    "required": false
   }
  ]
 },
 {
  "name": "publish_config_show",
  "group": "service",
  "mutates": false,
  "scope": "mcp:read",
  "doc": "Картка профілю публікації: канал, режим, відбір (теги/регіони/аудит), лічильники.",
  "summary": "Картка профілю публікації: канал, режим, відбір (теги/регіони/аудит), лічильники.",
  "params": [
   {
    "name": "ref",
    "type": "str",
    "doc": "Профіль: id або частина назви.",
    "default": null,
    "required": true
   }
  ]
 },
 {
  "name": "publish_config_update",
  "group": "service",
  "mutates": true,
  "scope": "mcp:write",
  "doc": "Змінити профіль публікації: увімкнути/вимкнути, канал, відбір, режим, AI.\n\nСписки (tags, require_tags, exclude_tags, regions) замінюються ПОВНІСТЮ\nпереданим значенням; '-' очищає; не передано = без змін. Не передані\nскаляри теж не змінюються.",
  "summary": "Змінити профіль публікації: увімкнути/вимкнути, канал, відбір, режим, AI.",
  "params": [
   {
    "name": "ref",
    "type": "str",
    "doc": "Профіль: id або частина назви (свій).",
    "default": null,
    "required": true
   },
   {
    "name": "name",
    "type": "str",
    "doc": "Назва профілю.",
    "default": "",
    "required": false
   },
   {
    "name": "task",
    "type": "str",
    "doc": "Дослідження (id/slug/назва — своє). '-' = будь-яке (усі свої події).",
    "default": "",
    "required": false
   },
   {
    "name": "chat_id",
    "type": "str",
    "doc": "Chat ID каналу Telegram: -100… або @username.",
    "default": "",
    "required": false
   },
   {
    "name": "bot_token",
    "type": "str",
    "doc": "Bot token (порожньо = глобальний з env). У відповідях не показується.",
    "default": "",
    "required": false
   },
   {
    "name": "is_active",
    "type": "bool",
    "doc": "Активний профіль (воркер публікує лише активні).",
    "default": null,
    "required": false
   },
   {
    "name": "review_status",
    "type": "str",
    "doc": "Публікувати події зі статусом: approved (дефолт) | pending | rejected.",
    "default": "",
    "required": false
   },
   {
    "name": "tags",
    "type": "str",
    "doc": "Теги-відбір (АБО): `кат:тег, кат:тег`. '-' = без обмеження.",
    "default": "",
    "required": false
   },
   {
    "name": "require_tags",
    "type": "str",
    "doc": "Обовʼязкові теги (І): `кат:тег, …`. '-' = очистити.",
    "default": "",
    "required": false
   },
   {
    "name": "exclude_tags",
    "type": "str",
    "doc": "Теги-виключення: подія з будь-яким із них не публікується. '-' = очистити.",
    "default": "",
    "required": false
   },
   {
    "name": "regions",
    "type": "str",
    "doc": "Субʼєкти РФ через кому (канонічна назва/аліас). '-' = усі.",
    "default": "",
    "required": false
   },
   {
    "name": "max_age_days",
    "type": "int",
    "doc": "Не публікувати старше N діб. 0 = без обмеження; -1 = не змінювати.",
    "default": -1,
    "required": false
   },
   {
    "name": "publish_from",
    "type": "str",
    "doc": "Публікувати події від дати YYYY-MM-DD. '-' = зняти.",
    "default": "",
    "required": false
   },
   {
    "name": "raw_mode",
    "type": "bool",
    "doc": "true — без ШІ: оригінальний текст + теги; AI-промпт тоді лише фільтр.",
    "default": null,
    "required": false
   },
   {
    "name": "ai_model",
    "type": "str",
    "doc": "AI-модель (порожньо = дефолт). '-' = скинути.",
    "default": "",
    "required": false
   },
   {
    "name": "ai_prompt",
    "type": "str",
    "doc": "AI-промпт (фільтр + рерайт). '-' = очистити (у raw_mode = LLM не викликається).",
    "default": "",
    "required": false
   },
   {
    "name": "max_per_pass",
    "type": "int",
    "doc": "Макс. постів за прохід воркера. 0 = не змінювати.",
    "default": 0,
    "required": false
   },
   {
    "name": "post_as_account",
    "type": "bool",
    "doc": "true — постити Telegram-акаунтом (з медіа), потрібен forward_account.",
    "default": null,
    "required": false
   },
   {
    "name": "forward_account",
    "type": "str",
    "doc": "Акаунт публікації (id/номер/назва — видимий тобі). '-' = зняти.",
    "default": "",
    "required": false
   }
  ]
 },
 {
  "name": "publish_status",
  "group": "service",
  "mutates": false,
  "scope": "mcp:read",
  "doc": "Профілі публікації + останні публікації подій у Telegram.",
  "summary": "Профілі публікації + останні публікації подій у Telegram.",
  "params": [
   {
    "name": "limit",
    "type": "int",
    "doc": "Скільки останніх публікацій показати.",
    "default": 10,
    "required": false
   }
  ]
 },
 {
  "name": "published_list",
  "group": "service",
  "mutates": false,
  "scope": "mcp:read",
  "doc": "Що було опубліковано (журнал `PublishedEvent`): коли, яким профілем, яка подія,\nпосилання на пост у каналі; для відсіяних/збійних — причина.\n\nПовний текст поста — `published_show`. Перечергувати подію (щоб воркер\nобробив наново) можна лише в адмінці (дія «Перечергувати»).",
  "summary": "Що було опубліковано (журнал `PublishedEvent`): коли, яким профілем, яка подія, посилання на пост у каналі; для відсіяних/збійних — причина.",
  "params": [
   {
    "name": "config",
    "type": "str",
    "doc": "Профіль: id або частина назви. Порожньо = усі свої.",
    "default": "",
    "required": false
   },
   {
    "name": "task",
    "type": "str",
    "doc": "Дослідження (id/slug/назва). Порожньо = усі.",
    "default": "",
    "required": false
   },
   {
    "name": "status",
    "type": "str",
    "doc": "published (дефолт) | skipped (відсіяно AI) | failed | pending | all.",
    "default": "published",
    "required": false
   },
   {
    "name": "days",
    "type": "int",
    "doc": "За останні N днів (за часом публікації/створення запису).",
    "default": 14,
    "required": false
   },
   {
    "name": "query",
    "type": "str",
    "doc": "Текст у пості або описі події.",
    "default": "",
    "required": false
   },
   {
    "name": "limit",
    "type": "int",
    "doc": "Скільки записів показати.",
    "default": 30,
    "required": false
   }
  ]
 },
 {
  "name": "published_show",
  "group": "service",
  "mutates": false,
  "scope": "mcp:read",
  "doc": "Одна публікація повністю: текст поста, вердикт і причина AI, посилання, помилка.",
  "summary": "Одна публікація повністю: текст поста, вердикт і причина AI, посилання, помилка.",
  "params": [
   {
    "name": "ref",
    "type": "str",
    "doc": "id запису публікації (з published_list).",
    "default": null,
    "required": true
   }
  ]
 },
 {
  "name": "service_health",
  "group": "service",
  "mutates": false,
  "scope": "mcp:read",
  "doc": "Загальний стан сервісу одним екраном: черги, збори, акаунти, джерела, публікація.\n\nПерше, що варто викликати: показує ЩО стоїть і ХТО це розгрібає.",
  "summary": "Загальний стан сервісу одним екраном: черги, збори, акаунти, джерела, публікація.",
  "params": []
 },
 {
  "name": "service_queues",
  "group": "service",
  "mutates": false,
  "scope": "mcp:read",
  "doc": "Черги конвеєра детально: стадії×задачі, застряглі claim'и, свіжі помилки.\n\ntask — id/slug/назва (порожньо = всі); stage — конкретна стадія;\nerrors — скільки прикладів помилок показати (0 = без них).",
  "summary": "Черги конвеєра детально: стадії×задачі, застряглі claim'и, свіжі помилки.",
  "params": [
   {
    "name": "task",
    "type": "str",
    "doc": "Задача: id, slug або частина назви. Порожньо = усі видимі.",
    "default": "",
    "required": false
   },
   {
    "name": "stage",
    "type": "str",
    "doc": "Стадія конвеєра: collected | enriched | preclustered | classified | deduped | mon_collected | mon_filtered | mon_prescreened | tgs_collected | tgs_screened | tgs_tagged | info_collected | info_screened | done | failed. Порожньо = усі непорожні.",
    "default": "",
    "required": false
   },
   {
    "name": "errors",
    "type": "int",
    "doc": "Скільки прикладів свіжих помилок показати (0 — без них).",
    "default": 3,
    "required": false
   }
  ]
 },
 {
  "name": "setting_set",
  "group": "service",
  "mutates": true,
  "scope": "mcp:admin",
  "doc": "Записати налаштування (`Setting`). Порожнє значення = дефолт із коду.\n\nКлючі не валідуються: одруківка не впаде з помилкою, а створить НОВИЙ\nнепотрібний рядок, і код далі читатиме старий ключ. Тому спершу звір ім'я\nчерез `settings_list`, а у відповіді дивись позначку «створено/оновлено».",
  "summary": "Записати налаштування (`Setting`). Порожнє значення = дефолт із коду.",
  "params": [
   {
    "name": "key",
    "type": "str",
    "doc": "Ключ налаштування (напр. digest_report_prompt). Неіснуючий буде створено.",
    "default": null,
    "required": true
   },
   {
    "name": "value",
    "type": "str",
    "doc": "Нове значення. ПОРОЖНЄ = повернутись до дефолту з коду.",
    "default": null,
    "required": true
   },
   {
    "name": "description",
    "type": "str",
    "doc": "Опис для адмінки. Порожньо = не чіпати наявний.",
    "default": "",
    "required": false
   }
  ]
 },
 {
  "name": "settings_list",
  "group": "service",
  "mutates": false,
  "scope": "mcp:read",
  "doc": "Key-value налаштування (`Setting`): промпти, тексти, прапорці без деплою.",
  "summary": "Key-value налаштування (`Setting`): промпти, тексти, прапорці без деплою.",
  "params": [
   {
    "name": "prefix",
    "type": "str",
    "doc": "Показати лише ключі, що містять цей текст.",
    "default": "",
    "required": false
   }
  ]
 },
 {
  "name": "tools_manifest",
  "group": "service",
  "mutates": false,
  "scope": "mcp:read",
  "doc": "Список інструментів MCP-шару (назва, група, чи змінює стан, параметри).",
  "summary": "Список інструментів MCP-шару (назва, група, чи змінює стан, параметри).",
  "params": []
 },
 {
  "name": "tz_channels",
  "group": "telezip",
  "mutates": false,
  "scope": "mcp:read",
  "doc": "/CHANNELS — пошук каналів і чатів у базі TeleZip (не в Телеграмі загалом).\n\nКанали, яких TeleZip не вантажить, тут не знайдуться. Межа саме цього\nендпоінта — 10 000 знайдених каналів; більше = відлуп, треба звужувати.\n(Не плутай із межею 20 000 для `channeltext` у tz_find — там канали лише\nвідбираються, а шукається все одно по повідомленнях.)\n\nВіддає: id, юзернейм, назву, опис, підписників, скільки повідомлень\nзбережено, мову, канал це чи чат, і чи він ЗАРАЗ у моніторингу (якщо ні —\nдоступна лише збережена історія). Плюс позначку, чи є він у нашому довіднику.\n\nЗвідси беруть `id`/`name` для `tz_find(channel=...)`.\n\nКожен виклик ≈ $0.10 (платиться за виклик, не за обсяг); наступна сторінка\nчерез `page_token` — це ще один виклик.",
  "summary": "/CHANNELS — пошук каналів і чатів у базі TeleZip (не в Телеграмі загалом).",
  "params": [
   {
    "name": "term",
    "type": "str",
    "doc": "Вільний пошук по назві, опису й юзернейму разом. Синтаксис як у tz_find: пробіл = І, АБО = |. Приклад: \"Якутия (новости | чат)\".",
    "default": "",
    "required": false
   },
   {
    "name": "name",
    "type": "str",
    "doc": "Юзернейм каналу, точний збіг, без @ (напр. sakhaday).",
    "default": "",
    "required": false
   },
   {
    "name": "title",
    "type": "str",
    "doc": "Пошук лише по НАЗВІ каналу (з відмінками, оператори працюють).",
    "default": "",
    "required": false
   },
   {
    "name": "about",
    "type": "str",
    "doc": "Пошук лише по ОПИСУ каналу.",
    "default": "",
    "required": false
   },
   {
    "name": "id",
    "type": "str",
    "doc": "Числові TelegramID каналів через кому.",
    "default": "",
    "required": false
   },
   {
    "name": "source",
    "type": "str",
    "doc": "Тип даних: telegram-канали (telezip, дефолт), зливи баз (darkzip) або обидва (all).",
    "default": "",
    "required": false
   },
   {
    "name": "page_size",
    "type": "int",
    "doc": "Скільки каналів на сторінку.",
    "default": 30,
    "required": false
   },
   {
    "name": "page_token",
    "type": "str",
    "doc": "Токен наступної сторінки з попередньої відповіді.",
    "default": "",
    "required": false
   },
   {
    "name": "timeout",
    "type": "int",
    "doc": "Скільки секунд чекати відповідь TeleZip (важкі запити — до 180+).",
    "default": 120,
    "required": false
   }
  ]
 },
 {
  "name": "tz_find",
  "group": "telezip",
  "mutates": false,
  "scope": "mcp:read",
  "doc": "Пошук у текстах повідомлень Telegram (`POST /v4/messages`).\n\nПотрібен хоча б один критерій: text, exact, regex, channel або user.\n\nПРОБІЛ = І, не АБО. `text=\"мигрант драка\"` вимагає ОБИДВА слова й на вузькій\nтемі тихо віддасть нуль. АБО — риска `|`, робоча форма:\n`text=\"(мигрант | приезж*) (драка | избил)\"`. Мінус ЗЛИТНО: `-mavic`.\nВідмінки застосовуються самі, перелічувати не треба.\n(Увага: конвеєрний збір `run_create` ходить у v3, де все навпаки —\nтам пробіл = АБО. Розмір і глибину індексу показує `tz_status`.)\n\nДва режими виводу:\n  * `stats=false` (типово) — самі повідомлення: тексти, автори, посилання.\n    Лічильник «віддано» = розмір ВИБІРКИ, а не скільки всього збігів;\n  * `stats=true` — лише цифри: скільки повідомлень, каналів, авторів,\n    динаміка по днях/годинах, топ каналів. Питай так обсяг ПЕРЕД тим, як\n    качати: працює на довгих вікнах, де видача відлупилась би.\n\nАЛЕ РЕЖИМИ РОЗБИРАЮТЬ ЗАПИТ ПО-РІЗНОМУ: пошук читає пробіл як І, статистика —\nяк АБО. Той самий рядок «(дэг | \"электронное голосование\") (сбой | зависл*)»\nдав 12 575 220 у stats і 4 228 у зметі (прод, 18-19.09.2026). Тому на запиті\nз І-групами статистикою обсяг НЕ міряють — вона рахує об'єднання слів; вона\nчесна лише на чисто-АБО запиті (самі синоніми через |).\n\nСтеля виводу: `limit` ≤ 10000 за виклик, `page_size` ≤ 1000. Якщо віддано\nрівно `limit` — вибірку обрізало: ділити вікно навпіл і качати половини.\n\nОдин виклик tz_find = один запит до TeleZip ≈ $0.10, незалежно від обсягу\n(`limit=10000` коштує стільки ж, скільки `limit=10`). Виняток — пагінація:\nкожна наступна сторінка через `page_token` оплачується окремо.",
  "summary": "Пошук у текстах повідомлень Telegram (`POST /v4/messages`).",
  "params": [
   {
    "name": "text",
    "type": "str",
    "doc": "ПОШУК ІЗ ВІДМІНКАМИ. ПРОБІЛ = І (не АБО!), АБО — вертикальна риска |: запит «мигрант драка» вимагає ОБИДВА слова, «мигрант | драка» — будь-яке. Робоча форма: групи через пробіл, синоніми всередині групи через |: «(мигрант | приезж*) (драка | избил | конфликт)». Відмінки самі: «дрон» ловить дрона/дрону/дронах — перелічувати не треба. Префікс: «мобилиз*» → мобилизация/мобилизовать. Фраза — у лапках: «\"сбор денег\"». Відстань — тильда ЗЛИТНО після фрази: «\"снаряд пушка\"~5». Виключення: «-mavic» або «-(всу фронт)», мінус ЗЛИТНО, без пробілу. Макроси кластерів лише в дужках: «+(##бавовна) -(##спам)». УВАГА: у діалекті бота й v3 (`task.telezip_query`, збір через run_create) усе НАВПАКИ — там пробіл = АБО, а І = +. Запит у чужому діалекті не падає з помилкою, а тихо віддає 0 збігів, і виклик усе одно коштує $0.10.",
    "default": "",
    "required": false
   },
   {
    "name": "days",
    "type": "int",
    "doc": "Скільки останніх діб узяти, включно з сьогоднішньою. Ігнорується, якщо задані date_from/date_to.",
    "default": 1,
    "required": false
   },
   {
    "name": "date_from",
    "type": "str",
    "doc": "Початок періоду YYYY-MM-DD. Треба разом із date_to.",
    "default": "",
    "required": false
   },
   {
    "name": "date_to",
    "type": "str",
    "doc": "Кінець періоду YYYY-MM-DD (включно). Треба разом із date_from.",
    "default": "",
    "required": false
   },
   {
    "name": "exact",
    "type": "str",
    "doc": "ДОСЛІВНО, без відмінків — для абревіатур, марок, серійних номерів: exact=«сво» не зачепить «свой»/«своего», а text=«сво» зачепить. Оператори ті самі, що в text. Можна разом із text: умови комбінуються (exact=\"сво\" + text=\"бригада\").",
    "default": "",
    "required": false
   },
   {
    "name": "regex",
    "type": "str",
    "doc": "Регулярний вираз по тексту повідомлення: картки, IBAN, телефони, крипто-гаманці, координати. Дорогий режим — звужуй вікно й канали.",
    "default": "",
    "required": false
   },
   {
    "name": "channeltext",
    "type": "str",
    "doc": "Фільтр ПО КАНАЛУ, а не по повідомленню: спершу відбираються канали, у назві/описі яких є ці слова, і лише в них шукається текст. Оператори як у text (пробіл = І, АБО = |). Приклад: channeltext=\"крым | крим | АРК\". Не став багато поширених слів: якщо під критерій підпаде понад 20 000 каналів, /FIND відлупить запит (у самого /CHANNELS межа інша — 10 000 знайдених каналів).",
    "default": "",
    "required": false
   },
   {
    "name": "channel",
    "type": "str",
    "doc": "Обмежити пошук цими каналами: @ім'я або числовий id, через кому. Порожньо = весь індекс (його розмір показує tz_status). Щоб узяти ВСЕ з каналу, лишай text=\"*\" і став сюди канал.",
    "default": "",
    "required": false
   },
   {
    "name": "user",
    "type": "str",
    "doc": "Обмежити пошук дописами цих авторів: @ім'я або числовий TelegramID, через кому. Так дивляться \"усе, що писала ця людина\".",
    "default": "",
    "required": false
   },
   {
    "name": "lang",
    "type": "str",
    "doc": "Мови повідомлень через кому, двобуквені коди як у гайді TeleZip: ru, uk, en, pl, ro… (мову визначає FastText-176). Порожньо = усі мови, і в вибірку лізе чуже. Для російського Telegram став \"ru\".",
    "default": "ru",
    "required": false
   },
   {
    "name": "hasmedia",
    "type": "bool",
    "doc": "true — лише повідомлення з фото/відео/документом; false — лише без медіа.",
    "default": null,
    "required": false
   },
   {
    "name": "unique",
    "type": "bool",
    "doc": "true згортає репости (лишає одну копію): менше даних, але ЗАНИЖЕНЕ охоплення. false віддає все, включно з репостами — тим самим запитом, другого виклику для цього не треба. У режимі stats=true параметр не діє (API його не приймає), тому лічильники stats рахують і репости: збір із unique=true дасть МЕНШЕ, ніж пообіцяв stats.",
    "default": true,
    "required": false
   },
   {
    "name": "source",
    "type": "str",
    "doc": "Тип даних: telegram-канали (telezip, дефолт), зливи баз (darkzip) або обидва (all).",
    "default": "",
    "required": false
   },
   {
    "name": "thread",
    "type": "int",
    "doc": "id top-повідомлення: звузити до ОДНІЄЇ гілки коментарів під постом.",
    "default": 0,
    "required": false
   },
   {
    "name": "extra",
    "type": "str",
    "doc": "JSON-обʼєкт із додатковими полями тіла запиту — запасний хід для того, що зʼявилось в API і ще не має власного параметра. Імена полів — camelCase з контракту v4 MessageSearchRequest (ті самі, що видно в рядку «критерії» у відповіді: searchTerm, exactTerm, channelTerm, channelIds, languages…); неіснуюче поле API просто відкине. Якщо потрібного імені не знаєш — не вгадуй, виклик усе одно коштує $0.10.",
    "default": "",
    "required": false
   },
   {
    "name": "stats",
    "type": "bool",
    "doc": "true — повернути ЛІЧИЛЬНИКИ (скільки повідомлень, каналів, авторів + динаміка) замість самих повідомлень. Ендпоінт /FindStats: працює там, де звичайний пошук відлупило б за обсягом, і не тягне тексти. УВАГА №1: цей режим розбирає запит як АБО, тоді як сам пошук — як І. На запиті з І-групами («(тема | тема2) (дія | дія2)») лічильники завищені на порядки й у топ лізуть чати знайомств і вантажоперевезень — вір їм ЛИШЕ на чисто-АБО запиті (самі синоніми через |), інакше обсяг питай самим зметом. УВАГА №2: `unique` тут не діє — API його не приймає.",
    "default": false,
    "required": false
   },
   {
    "name": "by",
    "type": "str",
    "doc": "Для stats=true: гранулярність динаміки — day або hour.",
    "default": "day",
    "required": false
   },
   {
    "name": "limit",
    "type": "int",
    "doc": "Стеля викачування повідомлень, до 10000 (більше API не віддає за виклик). Діє лише коли page_size=0: заданий page_size перебиває limit. Якщо «віддано» дорівнює limit — вибірку ОБРІЗАЛО: ділити вікно навпіл і качати половини окремими викликами (дешевше за сторінки).",
    "default": 200,
    "required": false
   },
   {
    "name": "sample",
    "type": "bool",
    "doc": "true — брати випадкову вибірку до limit замість перших N (швидше на важких запитах, результат не відтворюваний). Це ПРО ВИКАЧУВАННЯ; скільки з них показати текстом — окремий параметр samples.",
    "default": false,
    "required": false
   },
   {
    "name": "page_size",
    "type": "int",
    "doc": "Посторінкове викачування замість limit (взаємовиключні; якщо задане не 0 — limit ігнорується). МАКСИМУМ 1000, більше — 400 INVALID_PAGE_SIZE. Наступну сторінку бери з page_token, кожна сторінка — окремий виклик за $0.10, тож 20 тис. повідомлень це 20 сторінок ($2.00) проти 3-4 викликів з limit=10000 і поділом вікна ($0.40).",
    "default": 0,
    "required": false
   },
   {
    "name": "page_token",
    "type": "str",
    "doc": "Токен наступної сторінки з попередньої відповіді.",
    "default": "",
    "required": false
   },
   {
    "name": "samples",
    "type": "int",
    "doc": "Скільки вже викачаних повідомлень надрукувати текстом у відповіді (на ціну й на обсяг запиту не впливає). Не плутай із sample.",
    "default": 5,
    "required": false
   },
   {
    "name": "chars",
    "type": "int",
    "doc": "Обрізати текст кожного прикладу до N символів.",
    "default": 220,
    "required": false
   },
   {
    "name": "top",
    "type": "int",
    "doc": "Скільки каналів показати в топі.",
    "default": 15,
    "required": false
   },
   {
    "name": "timeout",
    "type": "int",
    "doc": "Скільки секунд чекати відповідь TeleZip (важкі запити — до 180+).",
    "default": 180,
    "required": false
   }
  ]
 },
 {
  "name": "tz_status",
  "group": "telezip",
  "mutates": false,
  "scope": "mcp:read",
  "doc": "Чи живий TeleZip: мережа, ключ, ГЛИБИНА індексу й лаг, слоти, свіжість збору.\n\n«Глибина» — найдавніша дата, яку взагалі можна шукати (searchDateLimit):\nглибше неї збір поверне порожньо не через помилку, а бо даних немає.",
  "summary": "Чи живий TeleZip: мережа, ключ, ГЛИБИНА індексу й лаг, слоти, свіжість збору.",
  "params": [
   {
    "name": "deep",
    "type": "bool",
    "doc": "true — додатково сходити в API по статистику індексу (глибина, лаг). Один безкоштовний службовий виклик.",
    "default": true,
    "required": false
   }
  ]
 },
 {
  "name": "tz_users",
  "group": "telezip",
  "mutates": false,
  "scope": "mcp:read",
  "doc": "/USERS — пошук людей за профілем: юзернейм, ім'я, прізвище, TelegramID.\n\nДва різні питання:\n  * `username` / `id` — точний збіг («хто такий @X», «чий це id»);\n  * `term` — вільний пошук по імені й прізвищу («усі Durov»).\n\nЩоб побачити, ЩО людина писала, візьми знайдений id у\n`tz_find(user=\"<id>\", text=\"*\")`.\n\nКожен виклик ≈ $0.10. Пошук за `username` і добір профілів — це два різні\nзапити, тож разом вийде $0.20.",
  "summary": "/USERS — пошук людей за профілем: юзернейм, ім'я, прізвище, TelegramID.",
  "params": [
   {
    "name": "username",
    "type": "str",
    "doc": "Юзернейми через кому, без @ — точний збіг. Віддає TelegramID навіть тоді, коли профілю в базі немає.",
    "default": "",
    "required": false
   },
   {
    "name": "id",
    "type": "str",
    "doc": "Числові TelegramID через кому.",
    "default": "",
    "required": false
   },
   {
    "name": "term",
    "type": "str",
    "doc": "Вільний пошук по UserName / FirstName / LastName (дослівно, без відмінків).",
    "default": "",
    "required": false
   },
   {
    "name": "is_bot",
    "type": "bool",
    "doc": "Лише боти (true) чи лише люди (false).",
    "default": null,
    "required": false
   },
   {
    "name": "is_active",
    "type": "bool",
    "doc": "Фільтр за ознакою активності профілю.",
    "default": null,
    "required": false
   },
   {
    "name": "page_size",
    "type": "int",
    "doc": "Скільки профілів на сторінку.",
    "default": 20,
    "required": false
   },
   {
    "name": "timeout",
    "type": "int",
    "doc": "Скільки секунд чекати відповідь TeleZip (важкі запити — до 180+).",
    "default": 120,
    "required": false
   }
  ]
 }
]